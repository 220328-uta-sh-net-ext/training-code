let greeting = require('./helloNode.js');

console.log(greeting('Bryan'));

// npm init creates package.json
// npm install will install any associated packages listed in package.json
// npm install packagename will install the specific package
// npm install -g packagename: installs a package globally
// npm install -D packagename: install a package as a dev dependencies